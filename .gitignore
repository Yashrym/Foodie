node_modules/
.next/
out/
build/
.DS_Store
*.local
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.vercel
*.tsbuildinfo
next-env.d.ts
.convex/

# Convex codegen output is committed so Vercel builds work even if the
# `convex deploy` codegen step gets skipped/cached. Local `npx convex dev`
# will keep these files in sync; you'll commit any diffs alongside your
# function changes.
# (Stale AI-agent scratch files inside _generated are still ignored.)
convex/_generated/ai/
