import { ProviderListings } from "@/components/provider/provider-listings";

export const metadata = { title: "Provider · Listings" };

export default function ProviderListingsPage() {
  return <ProviderListings />;
}
