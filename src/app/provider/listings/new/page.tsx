import { NewListingForm } from "@/components/provider/new-listing-form";

export const metadata = { title: "Provider · New listing" };

export default function NewListingPage() {
  return <NewListingForm />;
}
