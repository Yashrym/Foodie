import { ProviderReservations } from "@/components/provider/provider-reservations";

export const metadata = { title: "Provider · Reservations" };

export default function Page() {
  return <ProviderReservations />;
}
